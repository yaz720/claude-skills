# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# 评测逐次产出(本地临时、可重生、可能含隐私,不入库)
skills/*/evals/capability/outputs/

# 注意(以下都要入库,别忽略):
#  - dist/*.skill               供下载一键装
#  - evals/capability/golden/   黄金参考产出(评测可视基线)
#  - evals/RESULTS.md           分数历史(eval_run 自动追加)
